<!-- Claude Code entrypoint. The session-start protocol is single-sourced in
     agent/session-protocol.md (and rendered to every other tool by
     scripts/render-harness.sh). The @imports below expand both the protocol and
     the org entrypoint into context at launch — do not re-inline the protocol here. -->

@agent/session-protocol.md
@agent.md

<!-- ADOPTER_C03_EXTENSIONS -->
