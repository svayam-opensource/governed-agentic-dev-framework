# Projects

This folder contains all project workspaces for <ORG_NAME> agentic development projects.

Each subfolder corresponds to one project, named `PRJ-NNN-<slug>`.
Project workspaces are created by the `seed` script and live on project branches (`brnch-NNN-<slug>`).

See `registry.yaml` for the complete project registry.
See `knowledge/policies/agentic-development-policy.md` for governance rules.
