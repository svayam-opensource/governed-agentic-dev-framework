# {{ORG_NAME}} Project Registry
# Authoritative source for {{ORG_SLUG}}-NNN project numbering.
# The seed script reads last_issued, increments it, and updates this file.
# Never edit manually — use the seed script.

last_issued: 0
projects: []
