# Project Registry
# Authoritative source for PRJ-NNN project numbering.
# The seed script reads last_issued, increments it, and updates this file.
# Never edit manually — use the seed / manage scripts.

last_issued: 0
projects: []
