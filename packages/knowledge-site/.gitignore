# @svayam/knowledge-site — generated-face workspace
#
# POL-402 / C01: CONTENT IS NEVER COMMITTED. `content/` is a build-time
# symlink/rsync of the org SoT (svm-prj-work/knowledge). It is regenerated by
# scripts/prepare-content.mjs before every build and must never enter git.
content/
content
public/
public
.quartz-cache/

# deps & build noise
node_modules/
*.log
.DS_Store
